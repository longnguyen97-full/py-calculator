# How to install tkinter on Ubuntu
You can install it from the command line as follows.
`sudo apt install python3-tk`

Once you have installed it, you should be able to run `python 3` or `python` to check it.